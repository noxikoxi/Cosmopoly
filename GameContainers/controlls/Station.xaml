<UserControl x:Class="GameContainers.controlls.Station"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameContainers.controlls"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="150">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="{Binding StationName, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{StaticResource TextColor}" />
        <Border Grid.Row="1" Padding="10" >
            <Border
                 
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch" 
                Padding="20"
                CornerRadius="10"
                Background="#FF394856"
            >
                <Ellipse Fill="#FF414A4A" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                </Ellipse>
            </Border>
        </Border>

    </Grid>
</UserControl>

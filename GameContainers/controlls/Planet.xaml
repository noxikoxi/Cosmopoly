<UserControl x:Class="GameContainers.controlls.Planet"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameContainers.controlls"
             mc:Ignorable="d"
             MinWidth="0" MinHeight="0"
             ClipToBounds="False"
             >
    <Grid ClipToBounds="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Wiersz na właściciela -->
            <RowDefinition Height="*"/>
            <!-- Wiersz na elipsę -->
            <RowDefinition Height="Auto"/>
            <!-- Wiersz na nazwę planety -->
        </Grid.RowDefinitions>
        <Label 
            Content="{Binding PlanetOwner, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Foreground="{StaticResource TextColor}"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            FontSize="10"
            Panel.ZIndex="1"
            Grid.Row="0"/>
        <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" ClipToBounds="False">
            <Ellipse 
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch"
                Width="100"
                Fill="{Binding PlanetColor, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Panel.ZIndex="0"
                
            />
            <StackPanel ClipToBounds="False"  Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="ShipsContainer" MaxWidth="150" Height="60" Panel.ZIndex="1">
            </StackPanel>
            
        </Grid>
        <Label 
            Content="{Binding PlanetName, RelativeSource={RelativeSource AncestorType=UserControl}}" 
            Foreground="{StaticResource TextColor}" 
            FontSize="10"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Panel.ZIndex="1"
            Grid.Row="2"
        />
    </Grid>
</UserControl>
